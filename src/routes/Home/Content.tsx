import React from 'react'
import styled from 'styled-components'

export default () => (
  <Screen>
    Home
  </Screen>
)

const Screen = styled.div`
  display: flex;
  /* flex: 1; */
  flex-direction: row;
`